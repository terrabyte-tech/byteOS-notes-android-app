<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/customSaveDialog"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="20dp"
    android:layout_marginEnd="20dp"
    android:layout_marginLeft="20dp"
    android:layout_marginRight="20dp"
    android:layout_marginStart="20dp"
    android:layout_marginTop="20dp"
    android:fontFamily="@font/pinetype_titles">

    <EditText
        android:id="@+id/filename_input"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_marginEnd="10dp"
        android:layout_marginStart="20dp"
        app:layout_constraintEnd_toStartOf="@+id/fileTypeLabel"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:fontFamily="@font/pinetype_titles"

        android:textSize="14sp"
        android:ellipsize="end"
        android:hint="My note, Day 365, Pixel thoughts, etc."
        android:lineSpacingMultiplier="1.2"
        android:maxLines="1"
        android:inputType="text"
        android:scrollHorizontally="true"
        android:defaultFocusHighlightEnabled="false"
        app:backgroundTint="@color/tb_green_slighttrans"

        android:textColor="@color/tb_dark"
        android:textColorHint="@color/tb_dark_slighttrans"
    />

    <TextView
        android:id="@+id/fileTypeLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="20dp"
        android:fontFamily="@font/pinetype_titles"
        android:text=".txt"
        android:textColor="#333333"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
    />

</androidx.constraintlayout.widget.ConstraintLayout>